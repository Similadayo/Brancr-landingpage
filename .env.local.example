# Environment configuration for Brancr Landing Page
# Copy this file to `.env.local` and fill in the real values.
# `.env.local` is already ignored by git.

# Required: API key from Resend to send waitlist notification emails.
# Sign up at https://resend.com/ to obtain your key.
RESEND_API_KEY=pk_resend_xxxxxxxxxxxxxxxxxxxxx

# Required: base URL for the Brancr backend API (include protocol).
NEXT_PUBLIC_API_BASE_URL=https://api.brancr.com

# Required: comma-separated list of addresses that should receive waitlist signup alerts.
WAITLIST_NOTIFICATION_EMAIL=founder@brancr.com,team@brancr.com

# Optional: custom "from" header for notification emails. Defaults to "Brancr <waitlist@brancr.com>".
WAITLIST_FROM_EMAIL=Brancr <waitlist@brancr.com>

# Optional: custom directory for waitlist storage when running outside Vercel.
# Defaults to <project-root>/data. When deploying on Vercel the app automatically uses the temp directory.
# Example (Windows): WAITLIST_STORAGE_DIR=C:\Users\USER\AppData\Local\BrancrWaitlist
# Example (macOS/Linux): WAITLIST_STORAGE_DIR=/Users/you/.brancr/waitlist
WAITLIST_STORAGE_DIR=

# Optional: demo credentials used by the login page buttons.
NEXT_PUBLIC_DEMO_TENANT_EMAIL=demo-tenant@brancr.com
NEXT_PUBLIC_DEMO_TENANT_PASSWORD=changeme
NEXT_PUBLIC_DEMO_ADMIN_EMAIL=demo-admin@brancr.com
NEXT_PUBLIC_DEMO_ADMIN_PASSWORD=changeme

# Optional: Custom observability endpoint
# Defaults to /api/observability if not set
NEXT_PUBLIC_OBSERVABILITY_ENDPOINT=/api/observability

# Enable observability in development (default: only in production)
NEXT_PUBLIC_ENABLE_OBSERVABILITY=false

# Optional: WebSocket URL for real-time updates
NEXT_PUBLIC_WS_URL=wss://api.brancr.com/ws
